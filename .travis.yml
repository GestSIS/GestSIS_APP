language: node_js
node_js:
  - 12
cache: yarn
script:
  - yarn build

#before_script:
#  - echo -e "Host $PRODUCTION_SERVER\n\tStrictHostKeyChecking no\n" >> ~/.ssh/config

#before_deploy:
#  - openssl aes-256-cbc -K $encrypted_5b90fb13c77b_key -iv $encrypted_5b90fb13c77b_iv
#    -in id_rsa_gs.enc -out id_rsa_gs -d
#  - eval "$(ssh-agent -s)"
#  - chmod 600 id_rsa_gs
#  - ssh-add id_rsa_gs

#deploy:
#  - provider: script
#    skip_cleanup: true
#    script: rsync -r -q --delete-after ./dist/ $PRODUCTION_SERVER_USER@$PRODUCTION_SERVER:$FOLDER_DEPLOY
#    on:
#      branch: master
